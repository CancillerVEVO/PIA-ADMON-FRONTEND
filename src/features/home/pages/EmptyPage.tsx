export function EmptyPage() {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100%',
      }}
    >
      <h1
        style={{
          color: 'gray',
        }}
      >
        Select a group from the sidebar
      </h1>
    </div>
  );
}
